# 마비노기 모바일 거래소 감시

거래소 시세를 보고, 조건이 맞으면 알려주는 개인용 도구.

- **화면**: <https://scarzsearch-cyber.github.io/mobi-market/>
- **서버 감시**: 10분마다 자동 실행 (탭을 닫아도, 컴퓨터를 꺼도 알림이 옴)

---

## 지금 상태

| | 상태 |
|---|---|
| 화면 (시세·차트·천장 신호·제작 계산기) | ✅ 동작 |
| 서버 감시 워크플로 | ✅ 동작 (10분마다) |
| 감시 대상 설정 (`alerts/settings.json`) | ✅ 등재됨 — 파동의 영혼석 |
| **알림 발송** | ⏸ **`MOBI_API_KEY` 등록 대기** |

### 남은 한 가지

저장소 **Settings → Secrets and variables → Actions → New repository secret**

| 이름 | 값 |
|---|---|
| `MOBI_API_KEY` | 모비라이프 API 키. 여러 개면 줄바꿈/쉼표로 전부 넣으면 돌려 쓴다 |

이거 하나 넣으면 감시가 바로 돌기 시작한다. **키를 여러 개 갖고 있다면 화면에서 쓰는
것과 다른 키를 주는 게 좋다** — 같은 키를 둘이 나눠 쓰면 분당 30회 한도를 경쟁해서
화면에서 429 가 뜬다. 알림은 **GitHub 이슈 댓글 → 메일**로 온다
(추가 설정 없음 — `GITHUB_TOKEN` 은 워크플로가 이미 갖고 있다).

카카오톡으로 받고 싶으면 `KAKAO_REST_KEY` · `KAKAO_REFRESH_TOKEN` 을 더 넣으면 그쪽이
우선된다. **필수가 아니다** — 카카오는 앱 등록·로그인이 필요하고 토큰이 두 달마다
만료돼서 사람이 다시 갈아줘야 하므로, 기본 통로로 두지 않았다.

---

## 구조

| 파일 | 역할 |
|---|---|
| `index.html` | 화면 전부 (단일 파일, 빌드 없음, 바닐라 JS) |
| `alerts/watch.py` | 서버측 감시·판정·발송 |
| `alerts/settings.json` | 무엇을 어떤 조건으로 감시할지 |
| `alerts/state.json` | 봇이 관리 (main 아님 — `alerts-state` 브랜치) |
| `alerts/test_rules.py` · `test_flow.py` | 판정 규칙·전체 흐름 테스트 (시크릿 없이 실행됨) |
| `.github/workflows/alerts.yml` | 10분마다 실행 |

자세한 내용은 [`alerts/README.md`](alerts/README.md).

### 화면과 서버가 보는 것은 같다

둘 다 같은 규약으로 판정한다 (`index.html` 의 `bandPosPct` / `rebuyInfo` = `alerts/watch.py` 의 같은 이름 규칙).
화면은 탭이 열려 있을 때 더 빨리·브라우저 알림까지, 서버는 10분마다 카카오톡으로.

---

## 천장 모델 — 이 도구의 유일한 잣대

이 거래소는 **12시간마다 3일 거래 평균가를 재고, 판매가 상한(천장) = 평균 × 1.5** 다
(소유자 실측 4건: 삼림 22→33, 용비늘 289,346→434,019 는 정수까지 정확). 가격은 이 천장을 못 넘는다.
판매 수수료 15%, 구매 수수료 0, 되팔기 불가.

평균가는 앱이 **스스로 추정**하고(최근 72시간 종가를 팔린 수량으로 가중), 게임 값을 **한 번 입력**하면
그 아이템의 오차를 **보정 계수**로 학습해 그 뒤로는 자동으로 맞춘다. 12시간마다 손으로 넣을 필요가 없다.

| 신호 | 조건 | 뜻 |
|---|---|---|
| 🔴 천장근접 | 최저가 ≥ 천장의 90% | 물량이 말랐다 — 천장 근처에 걸어두면 팔린다 |
| ⚠ 품귀 | 수량 급감 | 미리 높은 가격에 뿌려둘 때 |
| 🟢 저가 | 최저가 ≤ 천장의 60% | 천장 대비 싸다 — 살 때 |
| 🔁 되사기 | 최저가 ≤ 내 판매가 × 0.85 | 수수료 떼고도 남는 구간 |

기준 %는 아이템별로 조절할 수 있다.

**평균가 세 겹** (`index.html` 의 `effectiveAvg` = `alerts/watch.py` 의 `effective_avg`, 같은 픽스처로 교차 검증):
1. **자동 추정** — API 는 "가장 싼 매물"만 기록하고 게임은 "실제 체결가"를 평균낸다. 24가지 추정법 중
   거래량 가중 72h 평균이 가장 나았지만 아이템에 따라 최대 15% 어긋난다 (`alerts/calibrate_avg.py`).
2. **보정 계수** — 게임 평균을 한 번 입력하면 (입력 ÷ 그때 추정) 을 저장해 곱한다. 오차가 아이템 고유
   성질(호가 두께)이라 계수가 비교적 안정적이라는 가정. 7일 넘으면 ⏰ 로 재입력을 권한다.
3. **관측 하한** — 최저가는 천장을 못 넘으므로 최근 6h `max(high)÷1.5` 는 평균의 확실한 하한. 추정이
   그 밑이면 끌어올리고 ⏰. (plateau 를 천장으로 *단정* 하진 않는다 — 큰 매물 하나가 7시간 눌러앉아
   가짜 plateau 를 만드는 걸 실측했다. 하한으로만 쓰면 언제나 안전하다.)

우선순위: 12시간 안에 입력한 값 > 추정×계수 > 미보정 추정. 카드에 `평균 31 보정` 처럼 출처가 보인다.

**지운 것** — 종합 신호 점수(백분위·공급 계수·TEMA), 변동성·신규매물·트레일링·평균대비 알림,
스코어 필터, 적중률·수량검증 탭. 천장 규칙이 밝혀진 뒤로는 "천장의 몇 %" 하나가 더 정확하고,
알림 설정에 오만 가지가 나오는 것이 소유자가 지적한 문제였다.
